&EXT_RESTART ON
RESTART_DEFAULT 		F
RESTART_FILE_NAME 		${RESTARTFILE} 
RESTART_POS 			T
RESTART_COUNTERS 		T
@IF ( ${RTYPE} == MD )
	RESTART_VEL 		T
  @IF ( ${MD_ENS} == NVT )
	RESTART_THERMOSTAT 	T 
  @ENDIF
@ENDIF
&END EXT_RESTART
